// Assert _01

'use strict';

let assert = require('assert');

// Basic Assert
assert(1 === 1);
// assert(1 === 2, 'that isn\'t right')

// Assert equals very simialr only more explicit.
// Shallow equality
assert.equal(1, 1);
assert.equal(1, 2);
